version: 2
builds:
  - main: ./cmd/server
    binary: swisseph-mcp
    env:
      - CGO_ENABLED=1
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

archives:
  - format: tar.gz

changelog:
  sort: asc
  filters:
    exclude:
      - "^chore:"
      - "^docs:"
